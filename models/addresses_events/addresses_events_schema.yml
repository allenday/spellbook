models:
- columns:
  - description: Name of the blockchain
    name: blockchain
    tests: []
  - description: Concerned address
    name: address
    tests: []
  - description: Which address first funded the address with the native asset on that
      chain
    name: first_funded_by
    tests: []
  - description: The block time on which the address was first funded with the native
      asset on that chain
    name: block_time
    tests: []
  - description: The block number on which the address was first funded with the native
      asset on that chain
    name: block_number
    tests: []
  - description: The transaction hash on which the address was first funded with the
      native asset on that chain
    name: tx_hash
    tests: []
  config:
    tags:
    - table
    - funded
    - addresses_events
    - ethereum
  description: Table showing who first funded each address in its native asset on
    every chain
  meta:
    blockchain: ethereum
    contributors: hildobby
    project: addresses
    sector: addresses_events
  name: addresses_events_first_funded_by
  tests: []
version: 2
