sources:
- name: ethereum
  tables:
  - description: signatures from ABIs on this chain
    freshness:
      warn_after:
        count: 12
        period: hour
    loaded_at_field: created_at
    name: signatures
- name: polygon
  tables:
  - description: signatures from ABIs on this chain
    freshness:
      warn_after:
        count: 12
        period: hour
    loaded_at_field: created_at
    name: signatures
- name: optimism
  tables:
  - description: signatures from ABIs on this chain
    freshness:
      warn_after:
        count: 12
        period: hour
    loaded_at_field: created_at
    name: signatures
- name: arbitrum
  tables:
  - description: signatures from ABIs on this chain
    freshness:
      warn_after:
        count: 12
        period: hour
    loaded_at_field: created_at
    name: signatures
- name: gnosis
  tables:
  - description: signatures from ABIs on this chain
    freshness:
      warn_after:
        count: 12
        period: hour
    loaded_at_field: created_at
    name: signatures
- name: bnb
  tables:
  - description: signatures from ABIs on this chain
    freshness:
      warn_after:
        count: 12
        period: hour
    loaded_at_field: created_at
    name: signatures
- name: avalanche_c
  tables:
  - description: signatures from ABIs on this chain
    freshness:
      warn_after:
        count: 12
        period: hour
    loaded_at_field: created_at
    name: signatures
- name: fantom
  tables:
  - description: signatures from ABIs on this chain
    freshness:
      warn_after:
        count: 12
        period: hour
    loaded_at_field: created_at
    name: signatures
version: 2
